# drawingidentifier
A handwritten/doodle recognition app: a Java Swing interface for sketching 48×48 pixel drawings, paired with a Python-trained deep neural network (using standard ML libraries) that classifies them in real time.
